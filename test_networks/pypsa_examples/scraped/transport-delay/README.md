# transport-delay

This is the scraped version of this link: https://docs.pypsa.org/latest/examples/transport-delay/

Run `python scrape_transport_delay.py` from this directory to scrape the page code, execute it, and save the finished network as `transport-delay.nc`.
