# scigrid-redispatch

This is the scraped version of this link: https://docs.pypsa.org/latest/examples/scigrid-redispatch/

Run `python scrape_scigrid_redispatch.py` from this directory to scrape the page code, execute it, and save the finished network as `scigrid-redispatch.nc`.
