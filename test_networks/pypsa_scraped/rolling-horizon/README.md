# rolling-horizon

This is the scraped version of this link: https://docs.pypsa.org/latest/examples/rolling-horizon/

Run `python scrape_rolling_horizon.py` from this directory to scrape the page code, execute it, and save the finished network as `rolling-horizon.nc`.
