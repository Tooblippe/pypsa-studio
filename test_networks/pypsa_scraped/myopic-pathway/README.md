# myopic-pathway

This is the scraped version of this link: https://docs.pypsa.org/latest/examples/myopic-pathway/

Run `python scrape_myopic_pathway.py` from this directory to scrape the page code, execute it, and save the finished network as `myopic-pathway.nc`.
