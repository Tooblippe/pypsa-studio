# example-1

This is the scraped version of this link: https://docs.pypsa.org/latest/examples/example-1/

Run `python scrape_example_1.py` from this directory to scrape the page code, execute it, and save the finished network as `example-1.nc`.
