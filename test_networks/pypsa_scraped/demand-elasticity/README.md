# demand-elasticity

This is the scraped version of this link: https://docs.pypsa.org/latest/examples/demand-elasticity/

Run `python scrape_demand_elasticity.py` from this directory to scrape the page code, execute it, and save the finished network as `demand-elasticity.nc`.
