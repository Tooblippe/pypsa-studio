# unit-commitment

This is the scraped version of this link: https://docs.pypsa.org/latest/examples/unit-commitment/

Run `python scrape_unit_commitment.py` from this directory to scrape the page code, execute it, and save the finished network as `unit-commitment.nc`.
